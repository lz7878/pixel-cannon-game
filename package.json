{
  "name": "pixel-cannon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare-levels": "python3 tools/build_level_catalog.py",
    "dev": "pnpm prepare-levels && vite",
    "build": "pnpm prepare-levels && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "pixi.js": "8.16.0"
  },
  "devDependencies": {
    "vite": "7.2.2"
  }
}
